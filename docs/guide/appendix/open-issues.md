# 未決事項、実装裁量、IR 抽出で見つかった問題

この付録は、まだ決めていない事項、実装する側が選んでよい事項、IR を抽出したときに見つかった問題を集める。
本文の章が断定を避けている箇所の背景を確かめるとき、実装の選択が契約の範囲に収まるかを判断するときに読む。

## 別の作業で判断する事項

次の 2 つは、今回の文章整理では決定しない。

- 同梱のプロファイルにある `ro ~/.codex/skills` は、codex の公式文書（2026-09-07 時点）がスキルの探索先として挙げていないパスである。
  公式文書が挙げているのは `.agents/skills`、`~/.agents/skills`、`/etc/codex/skills` である。
  この改訂の範囲外として、別の作業で保守側が決める。
  それまで、同梱のプロファイルは現行の jail の写しのままとする。
- [セットアップスキル](../reference/12-setup-skill.md)で、エージェント CLI でない包む対象に固有の手順。
  事例が出たときに、別の作業で決める。
  それまで、スキルは「包む対象のコマンド」の一般の手順だけを持つ。

## 実装する側が決めてよい事項

次の事項は、各章が定める振る舞いを保つ範囲で、実装する側が選べる。

| 事項 | 選べる範囲と、契約として残る条件 |
|---|---|
| 内部のモジュール構成 | 自由。TOML の読み込みに使うライブラリも選べる |
| seccomp フィルタを bwrap に渡すファイル記述子 | 作り方（パイプかメモリ上のファイルか）と番号の割り当てを選べる |
| 計画の表示の整形 | [計画の表示](../reference/04-plan.md)が定める内容を含むことだけが条件 |
| 警告と診断の説明部分の文言 | 接頭辞と種類、および[ポリシーを保護する配置](../reference/06-policy-placement.md)と[マウントの解決](../reference/07-mounts.md)の生成される項目の規則が検査ごとに要求する要素（多くは 3 つ、固定部分への着地の拒否は 2 つ）だけが契約 |
| 走査の探索順と並列化 | 結果は[マウントの解決](../reference/07-mounts.md)の順序の規則で並べ直し、走査の中で出る診断が名指すリンクはパスのバイト順で決まるので、順序は結果に影響しない |
| `rw-copy` の複製元を読む順と並列化 | 引数に並ぶ順（ディレクトリが先、同じディレクトリの中はエントリ名のバイト順）は[マウントの解決](../reference/07-mounts.md)の契約なので、どう読んでも結果は同じ |
| 制御文字の見える表記の形と、UTF-8 として不正なバイト列の表し方 | [入れ子、並列、出力](../reference/10-process.md)が定める範囲で選べる。どの形でも「1 行、制御文字なし」は保たれる |
| ポリシーを保護する配置と、生成される項目の診断に出すパスの形 | 書いたまま、展開後、実体のどれでもよい。検査ごとに要求する要素が含まれることだけが契約 |
| ポリシーを保護する配置で、根の項目の検査と露出する組の検査を項目をまたいでどう交互に行うか | 項目ごとに 2 つの検査を行ってから次の項目へ進むか、検査ごとに全項目を見るかを選べる。変わるのは最初に出る診断だけ |
| ポリシーを保護する配置の診断の細部 | 名指すリンクや書き込める項目が複数あるときの選び方、保護対象の 3 つの規則のどれを出すか、通ったディレクトリがディレクトリでない成分だったときの名詞、理由の文言を選べる（どれを出すかは[保護対象の診断順位が裁量の記述と競合する](#保護対象の診断順位が裁量の記述と競合する)で未整理） |
| `--print-plan` で組み込みの既定を使ったことを示す文言 | 文字列 `kakoi init` を含むことだけが契約（[計画の表示](../reference/04-plan.md)） |
| `--print-plan=json` の 1 行の中の空白と、オブジェクトのキーの順序 | [計画の表示](../reference/04-plan.md)が定めるキーと値の意味、1 行であることだけが契約 |
| `init` が `profile/` と `secrets/` を作る順と、書き込みの途中で失敗したときに作ったディレクトリを残すか消すか | どちらでも、次の `init` の結果は同じ |

実装する側は、ここに無い新しい観測可能な振る舞いを黙って選ばない。
必要になったら、契約（IR とこのガイド）に戻して決める。

## ネットワークの実装裁量

合意した挙動を変えない範囲で、内部の構造、関数名、内部の型、処理の手順と検証の方法は、担当する側が決める。
許可の範囲、安全性、対応環境、ポリシーの意味、公開 API の責務が変わる判断は、利用者へ戻す。
まだ実証していない条件を満たせなければ、[ライブラリの責務と実証条件](../maintainer/library.md)に従い、製品実装へ進まない。

## ネットワークの IR が未決としている事項

filtered の IR は、文書ごとに冒頭で、その文書がまだ決めていない点を挙げている。
本文の章が「決まっていない」と書いてリンクしている先はこの表である。
冒頭の注記には、後から追加した要求で決まった点を未決のまま残しているものがある。
その場合は表の右の列に、決めた要求がある章を示した。

| 章 | 未決の点 | 後から決まった点 |
|---|---|---|
| [通信許可](../reference/network/02-allow.md) | 内部アドレスの分類。モードを指定せず空の配列や補助の指定だけがある場合。使わない指定の項目間の整合の検査、空の指定、既定のモードとの整合。起動時のポリシーと上流の応答を照合する具体的な方法と、制御の仕組みを守る方法。ホストの localhost を指す固定名を提供する仕組みと中継の経路 | |
| [IP アドレス](../reference/network/03-addresses.md) | IPv4-mapped 以外の IPv4 埋め込み形式。内部と特殊用途のアドレスの分類、ホスト側への接続の具体的な形。インターフェースの指定の扱い。参照する分類表の版と、使えない宛先の細則。リンクローカルの接続口の名前の変更、隔離環境からの経路、広い CIDR で含む場合 | |
| [DNS による許可](../reference/network/04-dns.md) | 国際化ドメイン名の検査オプション、依存の版、計画に表示する項目。解決失敗を覚えておく時間の書き方、キャッシュのキー、否定応答や過負荷による拒否との区別。失敗応答の詳しい理由を知らせる形と、その他の入力の不正への応答。対応するレコード種別の完全な一覧。上流から受けたその他のエラー、全候補が失敗したときの応答、通信失敗の詳細と待ち時間 | |
| [上流 DNS と処理の上限](../reference/network/05-dns-upstream.md) | ホストの DNS 設定の取り方と対応環境、指定の形。設定の変更を検知する仕組みと遅延。変更後に設定を取れなかったときの回復の検知と、DNS の失敗応答の具体形。上流の切り替え条件、待ち時間、通信方式、具体的な記法。名前解決 1 件の境界とその他の資源の上限。重複した問い合わせの集約、開始時点、環境全体の同時処理数と総負荷の制限。通信方式ごとの時間の計り方。解決条件が同じかの判定。キャッシュ応答の数え方。同じ要求の再送の識別と保持する量 | |
| [公開](../reference/network/06-publish.md) | 待受の検出と転送の実証、同時に検出したときの優先順。待受アドレスでの絞り込みと通信モードとの整合。ホスト側のアドレスの記法、公開中に片側だけ障害が起きた場合、内側の転送先の選び方と中継の実現方法。再試行の指定の可否と試行の時間上限、ポートの再利用。同じ待受が続いていることを確かめる仕組み。待受の終了を検出する方法。UDP のソケットの検出と再利用、相手ごとの中継の状態。待受の識別方法、共有先の構成が変わったときの寿命、ポートの割り当て順。公開 URL の表示 | |
| [filtered の監督と終了](../reference/network/07-lifecycle.md) | Ctrl+C を届ける方式。終了要求の具体的な仕組み。猶予の前に行う遮断の時間上限、停止の仕組みとイベントの順序の実証。遮断を保証する仕組みの実証、起動に失敗したときの終了コード。異常終了の検知と回収の方式。通知の詳しい表示形式、書き込みを分離する仕組み、1 件の通知の最大長、失敗理由が同じかの判定 | 外部からの終了要求、終了猶予のキーの置き場所と期限の起点、復帰の試行の時間上限と、再発したときに待ち時間を戻すこと、再試行の間隔と回数、保持する通知の量の上限と出力が戻った後の扱いは、同じ章の要求で決まっている |

## IR 抽出で見つかった問題

以下は、本体の既存の仕様とテストを照合したときに見つかった事項である。
不具合の断定でも、新しい仕様の判断でもない。
今回、製品とテストの動作は変えていない。

問題の種類は次の 3 つで表す。

| 種類 | 意味 |
|---|---|
| 検証の不足 | 要求に対して、テストが確かめていない部分がある |
| 記述の矛盾 | 仕様の記述どうし、または記述と観測が食い違う |
| 記述の曖昧さ | 記述の範囲が定まらず、適否の判断が分かれる |

### 配置の診断の理由が一部しか検証されていない
<!-- @kotowari[FLAG-001:e8bf05e4] -->

種類は検証の不足で、[ポリシーを保護する配置](../reference/06-policy-placement.md)の保護対象の検査に関わる。

`tests/placement.rs` の `a_policy_file_inside_a_writable_area_is_rejected_with_the_three_reasons` は、`assert_path_diagnostic` に、問題のパスと書き込める項目のパスだけを渡す。
診断に求める 3 つ目の要素である「隔離の中から差し替えられるため」という理由は確かめていない。
テストに要求の対応が付いていても、理由の説明が欠ける退行をこのテストだけでは検出できない。

実装の不具合は確認していない。
後続の修正では、自由な説明文の字面を固定せず、契約上の理由を伝えていることを検証する。

### 保護対象の診断順位が裁量の記述と競合する
<!-- @kotowari[FLAG-002:b1d52798] -->

種類は記述の矛盾で、[ポリシーを保護する配置](../reference/06-policy-placement.md)の保護対象の検査に関わる。

旧仕様のポリシーを保護する配置の節は、保護対象の 3 つの規則を「この順」に検査し、最初に当たったものを出すと定める。
一方、旧仕様の実装裁量の節（この付録の[実装する側が決めてよい事項](#実装する側が決めてよい事項)）には、「保護対象の 3 つの規則のどれを出すか」が含まれる。
複数の規則に当たるときの診断が固定か裁量かで、2 つの記述が競合する。

今回はどちらにも寄せず、既存の記述を保持した。
利用者に見える診断の順位を決めるときに、整理が要る。

### シムの検出手順で command -v の説明が不正確
<!-- @kotowari[FLAG-003:015ad4c8] -->

種類は記述の矛盾で、[セットアップスキル](../reference/12-setup-skill.md)の実行可能ビットと経路の確認に関わる。

旧仕様は「`command -v` はシェルの関数と alias を見ない」と説明する。
しかし、この環境の Bash で関数 `kakoi_probe_function` を定義して `command -v kakoi_probe_function` を実行すると、パスではなく関数名を返した。
少なくとも「関数を見ない」という説明は成り立たない。

alias の扱いを含め、対象とするすべてのシェルでの確認はしていない。
関数と alias による横取りを別に調べる既存の手順は維持し、今回は新しい探索の手順を決めない。

### ro の既存テストの名前とコメントが古い
<!-- @kotowari[FLAG-004:29406b13] -->

種類は記述の曖昧さで、[ポリシーを保護する配置](../reference/06-policy-placement.md)の再露出の拒否に関わる。

`tests/placement.rs` の `an_ro_written_through_a_writable_link_is_accepted_wherever_it_lands_outside_hide_and_ro` の名前とコメントは、`ro` に `ro` を重ねる形も露出として拒否すると読める。
既存の仕様と `a_referenced_ro_replacing_a_lower_layer_ro_is_accepted` は、その形を許可する点で一致している。

assert どうしの動作の矛盾は確認していない。
後続の作業で、名前とコメントの更新を検討する。

### init の既存ファイルの拒否で、書かない範囲が曖昧
<!-- @kotowari[FLAG-005:85b84208] -->

種類は記述の曖昧さで、[コマンドライン](../reference/03-cli.md)の `init` の出力と上書きの拒否に関わる。

旧仕様のコマンドラインの文法の節は、書き出し先が既にあれば「何も書かず」に拒否すると定める。
一方、`src/init.rs` の `write_built_in_default` は、`secrets/` の作成と `chmod 0700` を済ませてから、書き出し先が既にあるかを判定する。
`tests/cli.rs` の `init_refuses_to_overwrite_an_existing_profile` は、プロファイルの内容が変わらないことだけを確かめる。
「何も」が出力ファイルだけを指すのか、ディレクトリの作成とモードの変更も含むのかで、適否が変わる。

今回の抽出では、この語の範囲を決めていない。
実測では、既存のプロファイルを保ったまま `path` と 125 で終了し、`secrets/` のモードが 0755 から 0700 に変わった。
製品の副作用は確認済みだが、それが契約違反かどうかは、文言の範囲の判断として残る。

### 通常の端末操作が許されることを直接検証していない
<!-- @kotowari[FLAG-006:96aec97e] -->

種類は検証の不足で、[端末の保護](../reference/09-terminal.md)の保護の範囲に関わる。

既存のテストは、`TIOCSTI` と x32 の拒否を確かめている。
しかし、端末サイズを取得する ioctl が許されることと、制御端末が維持されることを直接確かめるテストは、今回の対応付けでは見つからなかった。
拒否の対象を広げすぎて通常の端末操作を壊す退行を、検出しきれない可能性がある。
実際に壊れているという証拠ではない。

### 既存のテストが仕様より細かいことを確かめている
<!-- @kotowari[FLAG-007:2811d11e] -->

種類は検証の不足で、[ポリシーファイルの形式と合成](../reference/05-policy.md)のワイルドカードと、[マウントの解決](../reference/07-mounts.md)の生成される `hide` に関わる。

`tests/mounts.rs` の `a_name_the_isolation_planted_cannot_stall_a_pattern_with_many_stars` は、星の多いパターンが 255 文字の名前に一致しないという結果に加えて、1 秒以内に終わることを確かめる。
既存の仕様は照合の意味を定めるが、1 秒という上限は定めていない。

`a_mount_point_with_a_byte_that_is_not_utf8_does_not_empty_the_mount_list` は、UTF-8 でないマウント名を保持することを確かめる。
既存の仕様は、その入力の境界を明示していない。

両テストの動作は維持する。
特定の上限や表現を今回の新しい要求として追認しないため、これらのテストには印を付けていない。
テストが無いのではなく、テストと明文化された要求の対応が足りないという問題である。

### ホーム全体の複製が必ず上限を超えるとは限らない
<!-- @kotowari[FLAG-008:7c9ffcd0] -->

種類は記述の曖昧さで、[ポリシーを保護する配置](../reference/06-policy-placement.md)の広すぎる書き込みの拒否と、[マウントの解決](../reference/07-mounts.md)の複製の上限に関わる。

旧仕様のポリシーを保護する配置の節は、ホーム全体の `rw-copy` は複製の上限に当たって `path` で止まると断定する。
しかし複製の上限は 4096 エントリ、64 MiB であり、それ以内に収まる小さなホームまで必ず拒否されるとは導けない。

ホームの `rw-copy` を広さの拒否の対象から外す規則は保持する。
上限の超過が典型例の説明なのか、別の無条件の拒否を意図したものなのかは、今回の文章整理では決めない。

### JSON の出所の列挙がどの欄に適用されるか曖昧
<!-- @kotowari[FLAG-009:4f0cf0e5] -->

種類は記述の曖昧さで、[計画の表示](../reference/04-plan.md)の JSON の出所に関わる。

旧仕様の出力の節は、出所の `kind` を `profile` をはじめとする 8 種類で列挙する。
一方、`tests/cli.rs` の JSON のテストは、項目の `origin.kind` が `profile` と `secret` であること、`policy_sources[0].kind` が `file` であることを確かめる。
列挙が項目の `origin` だけを指すのか、`policy_sources` も含むのかが、原文では明確でない。

今回は `kind` の追加もテストの変更も行わず、内側の JSON の契約を明確にする課題として残す。

### 全形式と名付けた表示のテストが全量の形式を確かめていない
<!-- @kotowari[FLAG-010:0487e548] -->

種類は検証の不足で、[計画の表示](../reference/04-plan.md)の全量の表示に関わる。

`tests/cli.rs` の `rw-copy` の表示のテストは `every_form` という名前を持つが、実際の呼び出しは `summary` と `json` で、`full` を含まない。
全量の形式にも複製の説明と項目が出るという条件を、このテストだけでは確かめられない。
印とテスト名を、網羅の完全な証拠として扱わない。

## 旧仕様にだけあった規則

旧仕様を削除する前に、旧仕様にはあって IR には無い規則を洗い出した。
実装やテストがその規則のとおりに動いているものもあるが、IR の要求ではないので、このガイドの本文は契約として扱っていない。
ここでは旧仕様の文面の要点を残す。
要求にするかどうかは、件ごとに別の作業で決める。

### 省略時のモードの既定値
<!-- @kotowari[FLAG-011:6b99af42] -->

関わる章は[ポリシーファイル](../reference/05-policy.md)と[ネットワークモード](../reference/network/01-modes.md)。

旧仕様は、`network.mode` を省略したときは `"host"`、`env.mode` を省略したときは `"inherit"` と定めていた。
IR にはどちらの既定値も無い。
同梱プロファイルは両方を明示しているので、既定値が効くのは同梱プロファイルを使わない構成だけである。

### カレントディレクトリを取得できないときの診断
<!-- @kotowari[FLAG-012:000c62a2] -->

関わる章は[プロセスと終了コード](../reference/10-process.md)の検査の段階。

旧仕様は、段階 3（カレントディレクトリの取得）で失敗したときを種類 `path` の診断としていた。
IR は段階の順序だけを定めている。
`tests/cli.rs` には、削除されたカレントディレクトリで `path` の診断になることを確かめるテストがある。

### bwrap 自身の exec の失敗
<!-- @kotowari[FLAG-013:f5195023] -->

関わる章は[プロセスと終了コード](../reference/10-process.md)。

旧仕様は、bwrap を見つけた後に消された場合や実行できない場合など、bwrap 自身の exec に失敗したときを種類 `bwrap`、終了コード 125 としていた。
まだ `kakoi` が動いている時点の失敗だからである。
一方、包んだコマンドの exec の失敗は、通常の起動では bwrap の失敗としてそのまま返り、入れ子では `command not executable` になる。
旧仕様はこの非対称を許容すると書いていた。
IR が種類 `bwrap` と定めるのは、bwrap が `PATH` に無い場合と、渡すファイル記述子を用意できない場合だけである。

### init が書く範囲
<!-- @kotowari[FLAG-014:4dbf3c74] -->

関わる章は[コマンドライン](../reference/03-cli.md)と[実行時の境界](../maintainer/runtime.md)。

旧仕様は、`init` をファイルを書く唯一の形とし、書く先を次に限っていた。

- 設定ディレクトリの規則で組み立てたパスの各成分（無ければ作る）
- その下の `profile/` と `secrets/`
- 書き出すファイル

また、`init` は `/tmp/kakoi` を作らないと定めていた。
同梱プロファイルのコメントも、`/tmp/kakoi` は kakoi が作らないと書いている。

### 段階 7 の内訳の細部
<!-- @kotowari[FLAG-015:f27d0687] -->

関わる章は[プロセスと終了コード](../reference/10-process.md)の段階 7 の内訳。

旧仕様は、3 番目の生成の中で、走査したリンクの先にある差し替えられる `ro` と、マウント一覧の読み込みの失敗も検査するとしていた。
9 番目の `rw-copy` の読み込みでは、対象の種類、読めない複製元、量の上限を検査するとしていた。
IR はそれぞれ「生成」「rw-copy の読み込み」とだけ書いている。

### 明示する上流 DNS の候補の書き方
<!-- @kotowari[FLAG-016:ca7e9bc7] -->

関わる章は[上流 DNS と処理の上限](../reference/network/05-dns-upstream.md)。

旧仕様は `[[network.dns-upstream]]` の各候補を次の形で書くと定めていた。

```toml
[network]
mode = "filtered"

[[network.dns-upstream]]
transport = "tls"
ip = "192.0.2.53"
port = 853
tls-name = "resolver.example.com"
```

| キー | 旧仕様の規則 |
|---|---|
| `transport` | 必須。通常の DNS の `plain` か `tls` |
| `ip` | 必須。IP アドレスそのもの |
| `port` | 必須。1〜65535 の整数 |
| `tls-name` | `tls` では必須。国際化名は ASCII に正規化する。ワイルドカードは書けない。`plain` に書くとエラー |

候補は書いた順と重複をそのまま保つ。
IR は、接続する IP と証明書の照合名を分けることだけを定めている。

### ホスト DNS の細部
<!-- @kotowari[FLAG-017:0bf56ab3] -->

関わる章は[上流 DNS と処理の上限](../reference/network/05-dns-upstream.md)。

旧仕様は次の 3 点を定めていた。

- 上流への問い合わせは kakoi 自身のネットワークから出るので、ホストのループバック上の DNS も使える。
- ホストの名前解決設定の内容が変わったことを、変更として検出する。
- 問い合わせ先の IP と通信方式が同じでも、設定の変更後は別の解決条件としてやり直せる。

IR は変更を検知する仕組みを未決としており、この 3 点を書いていない。

### 許可の登録のやり直しと、TTL を揃えない応答
<!-- @kotowari[FLAG-018:4cb07880] -->

関わる章は[DNS による許可](../reference/network/04-dns.md)。

旧仕様は、許可の登録が見込んだ時間に間に合わなければ、見込みを倍にして登録し直す（上限は残り時間の半分）と定めていた。
IR は見込む時間を延ばして登録し直すことだけを定める。

旧仕様は、TTL を揃えずに元のまま返す署名付きの応答を TSIG と書いていた。
IR は「メッセージ署名付き」とだけ書き、許可の判断に関わる「署名付き」が DNSSEC と TSIG のどちらを指すかは IR からは決まらない。

### 固定公開の細部
<!-- @kotowari[FLAG-019:b4d82fd3] -->

関わる章は[公開](../reference/network/06-publish.md)。

旧仕様は固定公開について次を定めていた。

- ホスト側は `127.0.0.1` または `::1` だけで待ち受ける。
- 内側の転送先は指定した系統のループバックで、そのアドレスに接続できる全アドレスの待受も使える。
- 公開の一部だけが成功した状態では起動しない。
- 設定の一覧や外部コマンドの成功終了だけを、ポートを実際に確保した証拠にしない。
- 環境の終了時は、既存の終了順序に従って公開を止めて回収する。
- アプリの標準出力は書き換えない。

### 固定公開の既知の制約
<!-- @kotowari[FLAG-020:a4342914] -->

関わる章は[公開](../reference/network/06-publish.md)。

旧仕様は、初版が受け入れる既知の制約として次の 2 つを載せていた。

- pasta は UDP の流れごとに、ホスト側のソケットにアプリ側の送信元ポートと同じ番号を使う。
  その番号がホストの別のソケットで使われていると、pasta はその流れを作れず、同じ送信元からのデータグラムを捨て続ける。
  アプリからは応答の無いタイムアウトに見え、kakoi は通知しない。
  送信元ポートを変えた再送は別の流れとして扱われる。
- ホスト自身が持つアドレスは起動時に一度だけ読む。
  起動後にホストが得たアドレス（VPN の接続で得たものが典型）はホストのアドレスとして扱わず、`dns` の許可だけで開きうる。

### セットアップスキルの対象と init の出力
<!-- @kotowari[FLAG-021:37f62ae3] -->

関わる章は[セットアップスキル](../reference/12-setup-skill.md)。

旧仕様は、スキルが提案する対象にエージェント CLI 以外のコマンドも含むとしていた。
また、`kakoi init` が書き出すのは組み込みの既定そのもので、`full` の計画に出る合成後のポリシーと同じ内容だとしていた。

### シムの引数の写しと境界
<!-- @kotowari[FLAG-022:1e60d810] -->

関わる章は[シム](../reference/11-shim.md)。

旧仕様は、引数の写しの取りこぼしは隔離が緩む向きに働かない（写し先が足りず、対象が書けない側に倒れる）と書いていた。
一方、同梱の雛形のヘッダーは、写すオプションと同じ名前の語がどこにあってもそのオプションとして読むので、境界が広がりうると書いている。
その例は、`codex -m --cd /etc exec` で `--workspace /etc` が `kakoi` に渡るものである。
取りこぼしと誤読は別の現象だが、旧仕様の文を雛形全体の性質として読むと、雛形の記述と矛盾する。
実装の挙動は確かめていない。

### 公開文書の掲載内容の細目
<!-- @kotowari[FLAG-023:cea9a0ad] -->

関わる章は[公開文書に載せる事項](../maintainer/public-docs.md)。

旧仕様は、`docs/shim.md` に載せるものとして次を挙げていた。

- 雛形を写してツール節を埋める手順
- 同梱の値が codex の例であること
- 壊れ方と直し先の表
- 最初に入りそうな例
- 保守側と利用者が確かめる条件

README のシムの節には、雛形を写す最短の手順、`KAKOI_SHIM_OFF=1` があること、`docs/shim.md` への導線を載せるとしていた。
旧仕様の中の「README に書く」は、README と、README から直接リンクする詳細文書のどれかに書くことを指すとしていた。

### codex 以外のツール節
<!-- @kotowari[FLAG-024:114feefa] -->

関わる章は[kakoi とは](../reference/01-overview.md)の対象外。

旧仕様は、0.3 で作らないものに、codex 以外のツール節の値（opencode や claude のもの）を挙げていた。
実測していないものは同梱せず、利用者が雛形を写してツール節を埋める。

### ネットワークの入力エラーの診断
<!-- @kotowari[FLAG-025:8c3e576c] -->

関わる章は[ネットワーク](../reference/network/01-modes.md)の各章。

filtered の IR は、ポリシーの誤りを「入力エラー」「設定エラー」「起動前エラー」、起動時の失敗を「起動エラー」とだけ書き、診断の種類と終了コードを定めていない。
本体の診断の規則から `policy` と 125 を当てはめる読み方はあるが、IR は明示していない。
旧仕様にも明示は無かった。

### IR が旧仕様の節の一覧を参照している
<!-- @kotowari[FLAG-026:06745a3c] -->

関わる章は[検証の契約](../maintainer/verification.md)と[公開文書に載せる事項](../maintainer/public-docs.md)。

検証の要求の一部は「旧仕様の第 15.1 節の全項目」「第 15.2 節の全観測」を、公開文書の要求は「第 16 節の既知の隙間 15 件」を参照している。
旧仕様の削除後、これらの一覧の本文はガイドのこの 2 章にだけある。
ガイドは正本ではないので、一覧を IR へ移すか、要求の文言を一覧に依存しない形にする必要がある。
