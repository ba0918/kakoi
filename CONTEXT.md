# 用語集

意味の典拠は `docs/spec/kakoi.md` の第 2 節。このファイルは、各語の読みと、その意味で使わない
言葉を記録する。「使わない」は、続けて書いた意味で使わないという限定付きの禁止である。仕様が固定した
診断の種類（`policy`、`path` など）は契約の文字列であり、ここには載せない。

## 隔離

プロセスを実行するとき、利用者が安全を確保できるかどうかの境界。0.2 ではファイルシステム、
ネットワーク、環境変数、認証情報の 4 次元。プロセス ID、IPC、UTS、ユーザーの名前空間は必ず切る
（ユーザー名前空間を作れない環境では起動が拒否される）。cgroup 名前空間はカーネルが対応しない場合には
切らない。

使わない: 「サンドボックス」を bwrap の隔離の意味で。「jail」を隔離の意味で（現行の bash
スクリプトを指すときだけ使う）。

## ポリシー

`kakoi` が bwrap の引数列と環境を組み立てるのに使う、段の合成を終えた指定の内容。

使わない: 「設定」「config」「コンフィグ」をポリシーの意味で。「jail.conf」をポリシーの意味で
（現行スクリプトのマウント表を指すときだけ使う）。

## ポリシーファイル

ポリシーを書いた TOML ファイル一般。プロファイルもポリシーファイルの一種。

使わない: 「設定ファイル」をポリシーファイルの意味で。

## プロファイル

グローバルスコープにある名前付きのポリシーファイル。`--profile NAME` で指定する。
置き場所は設定ディレクトリの `profile/`。

使わない: 「プロファイル」を `--policy-file` で指定したファイルの意味で。

## 組み込みの既定

バイナリに埋め込まれた同梱プロファイルの内容。`--profile` が `default`（省略時を含む）で
`profile/default.toml` が無いときにだけ、その代わりにグローバルスコープになる。
`kakoi init` がファイルに書き出す。

使わない: 「既定のプロファイル」を、ファイルとして置かれた `default.toml` と区別せずに。
「フォールバック」。

## シム

利用者が `PATH` に置く薄いスクリプト。包む対象のコマンドを既定で `kakoi` に通し、対象に
固有の差（自前のサンドボックスを切るフラグ、引数の写し、利用者が承認した素通しの例外）を閉じ込める。
製品の一部ではなく、雛形（汎用の本体 + 先頭のツール節）を同梱する。

使わない: 「ラッパー」「ジェイル」をシムの意味で。「シム」を `kakoi` 自体の意味で。「分類」を
シムがサブコマンドの隔離の要否を決める意味で（既定は全部隔離で、素通しは例外。却下した案を名指す
ときを除く）。

## モデルが動かない

シムの許可リスト（素通し）に入れる前に利用者が確かめる性質。そのサブコマンドの実行中に、包む対象が
モデルに問い合わせず、モデルの出力でコマンドを実行する機能を持たないこと（仕様 2 節、16 節）。

使わない: 「安全」「無害」をこの意味で。

## セットアップスキル

同梱の SKILL.md。エージェント CLI に読ませて、利用者の環境に固有の設定（プロファイルへの追加、
シムの設置、代替コマンド、隔離の中で壊れたコマンドの扱い）を提案させる。提案は差分として利用者が承認してから書く。

使わない: 「インストーラ」。「セットアップ」を `init` の意味で。

## ホームディレクトリ

ホストの環境変数 `HOME` の実体のパス。無い・空・相対・実体を得られない・ディレクトリでない `HOME` は `env` の診断（仕様 13 節の段階 4）。

使わない: 「ホーム」を `~` の字面や `/home/<user>` の慣習の意味で。

## 設定ディレクトリ

`$XDG_CONFIG_HOME/kakoi/`、未設定（空や絶対パスでない値を含む）なら `~/.config/kakoi/`。プロファイルの置き場
`profile/` と、常に隠される秘密ファイルの置き場 `secrets/` を持つ。

使わない: 「プロファイルディレクトリ」を設定ディレクトリの意味で。

## 正本

利用者が編集する元のファイル。設定ディレクトリの中身やシムの置き場が、そこから生成や同期で作り直される
か、そこへのリンクであるとき、その元。セットアップスキルは、正本が他所にある場所には書かず、正本に入れる
内容を提案する。

使わない: 「実体」を正本の意味で（他所から生成して置いたファイルは設定ディレクトリの中にあるが、その
正本は他所にある）。

## 段

ポリシーの項目に優先順位を与える 4 つの層。グローバルスコープ、プロセススコープ、コマンドライン、
生成。下 3 つが書かれた段。上の段が下の段に足すか上書きする。

使わない: 「レイヤ」「継承」「include」を段の合成の意味で。「段」を手順の順番の意味で（そちらは
「段階」）。

## グローバルスコープ

一番下の段。`--profile` で選んだプロファイルが与える。

使わない: 「グローバル設定」「ユーザー設定」をこの段の意味で。

## プロセススコープ

下から 2 番目の段。`--policy-file` で指定したポリシーファイルが与える。

使わない: 「プロジェクト設定」「ローカル設定」をこの段の意味で。

## ワークスペース

変数 `${workspace}` の値で、ワークツリーを導出する起点。`--workspace` で指定、省略時は
カレントディレクトリ。それ自体はマウントされない。

使わない: 「cwd」「カレントディレクトリ」をワークスペースの意味で（カレントディレクトリは bwrap の
作業ディレクトリであり、ワークスペースとは別物）。「隔離の対象」（マウントされると誤読される）。

## ワークツリー

ワークスペースから親へ向かって最初に `.git` を持つディレクトリ。無ければワークスペース自身。

使わない: 「リポジトリ」「プロジェクトルート」をワークツリーの意味で。

## 指令

マウント表の 1 行の種類。`rw`、`rw-file`、`rw-copy`、`ro`、`hide` の 5 つ。

使わない: 「ディレクティブ」「ルール」「エントリ」を指令の意味で。

## 解決が参照するもの

あるパスを実体へ解決する途中で、成分を探すために中身を見るディレクトリの実体（`..` で戻る前に通った
ものを含む）と、辿るシンボリックリンクの置き場（親ディレクトリの実体とリンクの名前）。そのパス自身の
実体は含まない。

使わない: 「経路」「チェーン」「辿ったもの」を参照するものの意味で。

## 根の項目

書き込める項目（合成後の `rw`・`rw-file` の項目）のうち、自身の解決が書き込める項目の中（自身の中を
含む）にあるものを一切参照しないもの。隔離の中からの書き換えで解決の結果を変えられない。`rw-copy` は
書き込める項目に含めない。中への書き込みはホストに届かず、次の起動が読むものを変えられないためである。

使わない: 「錨」「アンカー」「信頼できる項目」を根の項目の意味で。

## 露出する組

書き込める項目の中を参照して解決される項目と、それが無効にする `hide`・`ro`・`rw-copy` の項目の組の
うち、新しく見えるものや書けるものが生まれるもの。項目が `rw`・`rw-file` で相手が
`hide`・`ro`・`rw-copy` のとき、および項目が `ro`・`rw-copy` で相手が `hide` のとき。相手には書かれた
項目のほか、祖先として効いている生成された `hide` も含む。`hide` の項目は何も露出しないので、どの相手
とも組にならない。

使わない: 「衝突」「上書きの組」を露出する組の意味で。

## 走査

`mounts.scan` に従って起点以下を名前で探し、`hide` の項目を生成すること。

使わない: 「スキャン」「検索」を走査の意味で。

## 秘密

`secrets` の環境変数名とファイルの組、およびその値。

使わない: 「シークレット」「クレデンシャル」「トークン」を秘密の意味で。

## 計画

ポリシーと収集した事実から算出した、bwrap の引数列（ファイル記述子の位置は記号）、環境、各マウント
項目の適用結果、解決したコマンドのパスの組。`--print-plan` が表示する。

使わない: 「ドライラン」「プレビュー」を計画の意味で。

## 診断

`kakoi` 自身の失敗を伝える標準エラーへの 1 行。`kakoi: <種類>: <説明>` の形。

使わない: 「エラーメッセージ」「ログ」を診断の意味で。警告（`kakoi: warning:`）は診断ではない。

## 入れ子

環境変数 `KAKOI=1` の状態で `kakoi` が起動されること。

使わない: 「再帰」「ネスト」を入れ子の意味で。
