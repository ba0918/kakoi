[package]
name = "process-wrap"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Run a command inside a bubblewrap mount namespace shaped by a layered policy."
license = "MIT"
repository = "https://github.com/ba0918/process-wrap"
include = [
    "/src/**",
    "/examples/**",
    "/Cargo.toml",
    "/Cargo.lock",
    "/LICENSE",
    "/README.md",
    "/CHANGELOG.md",
]

[dependencies]
clap = { version = "=4.6.6", features = ["derive"] }
libc = "=0.2.189"
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
toml = "=1.1.5"
